---
paths:
  - 'resources/**'
---

# Resources

## Keep custom surfaces theme-aware
Custom sidebar and preview surfaces must use DaisyUI base tokens for the light default and explicit dark overrides. Avoid hardcoded dark-only backgrounds around adaptive logos or controls, and keep active device controls visibly distinct in both themes.
